import { AddressFormShowFieldPipe } from './address-form-show-field.pipe';

describe('AddressFormShowFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressFormShowFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
