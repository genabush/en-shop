import { SortProductBySizePipe } from './sort-product-by-size.pipe';

describe('SortProductBySizePipe', () => {
  it('create an instance', () => {
    const pipe = new SortProductBySizePipe();
    expect(pipe).toBeTruthy();
  });
});
