import { DeliveryModesSelectedCardPipe } from './delivery-modes-selected-card.pipe';

describe('DeliveryModesSelectedCardPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryModesSelectedCardPipe();
    expect(pipe).toBeTruthy();
  });
});
