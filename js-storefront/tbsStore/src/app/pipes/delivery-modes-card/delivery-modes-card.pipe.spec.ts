import { DeliveryModesCardPipe } from './delivery-modes-card.pipe';

describe('DeliveryModesCardPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryModesCardPipe();
    expect(pipe).toBeTruthy();
  });
});
